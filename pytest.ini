[pytest]
markers =
    live: tests that make real network/service calls (run manually only)
    oracle: tests that cross-check analyzer output against tshark (requires tshark)
    integration: tests that need the compose Postgres and/or Kafka (run manually only)
