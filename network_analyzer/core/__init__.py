"""Domain models and pure format transforms.

This package must never import ``network_analyzer.messaging`` or
``network_analyzer.clients``. Only the Python standard library and
``network_analyzer.exceptions`` are allowed.
"""
